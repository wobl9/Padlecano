# iosApp

Xcode host for the iOS client. Full setup is planned in the **ios-host** migration phase.

The iOS app will embed `:composeApp` as a framework and render shared Compose UI via `ComposeUIViewController`.
